# Optional: real OpenAI chat in demo (otherwise mock LLM)
# OPENAI_API_KEY=sk-...
# OPENAI_CHAT_MODEL=gpt-4o-mini

# Semantic cache
SEMANTIC_CACHE_THRESHOLD=0.92
SEMANTIC_CACHE_TTL=3600
SEMANTIC_CACHE_MODEL=all-MiniLM-L6-v2

# Set to "mock" for fast tests without torch
# SEMANTIC_CACHE_EMBEDDER=mock
